# 🚀 Деплой Frontend на Vercel

Подробная инструкция по деплою BaseChainVerify frontend на Vercel.

## 📋 Подготовка

### 1. Убедитесь, что код запушен на GitHub

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push
```

### 2. Проверьте структуру проекта

Убедитесь, что структура правильная:
```
frontend/
├── src/
│   ├── app/
│   ├── components/
│   └── config/
├── package.json
├── next.config.js
├── tsconfig.json
└── tailwind.config.js
```

## 🔧 Настройка Vercel

### Шаг 1: Создайте проект на Vercel

1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `basechainverify`

### Шаг 2: Настройте проект

**Важные настройки:**

- **Framework Preset:** `Next.js` (автоматически определится)
- **Root Directory:** `frontend` ⚠️ **ВАЖНО!**
- **Build Command:** `npm run build` (или оставьте по умолчанию)
- **Output Directory:** `.next` (или оставьте по умолчанию)
- **Install Command:** `npm install` (или оставьте по умолчанию)

### Шаг 3: Добавьте Environment Variables

Нажмите **"Environment Variables"** и добавьте все переменные из `frontend/.env.local`:

```
NEXT_PUBLIC_CHAIN_ID=84532
NEXT_PUBLIC_NETWORK=sepolia
NEXT_PUBLIC_VERIFY_CORE_ADDRESS=0xF2EdCe99dFe0A006D062cdB8C120D6d02cAD4369
NEXT_PUBLIC_ISSUER_REGISTRY_ADDRESS=0xFE43ac5d3c843284032964BcC86F8Bf7d1C5c14b
NEXT_PUBLIC_CERTIFICATE_NFT_ADDRESS=0x26b7F0D2b0d4f5bA06dfF2b0FF6B8d10a33431b4
NEXT_PUBLIC_API_URL=https://your-backend-url.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=34fb4f28e5a532287738d1b97aea5cfb
```

**Важно:** 
- Для production используйте `NEXT_PUBLIC_CHAIN_ID=8453` (Base Mainnet)
- `NEXT_PUBLIC_API_URL` должен указывать на ваш задеплоенный backend

### Шаг 4: Деплой

Нажмите **"Deploy"**

## 🐛 Решение проблем

### Ошибка: "Cannot find module"

**Проблема:** Vercel не может найти файлы

**Решение:**
1. Убедитесь, что **Root Directory** установлен в `frontend`
2. Проверьте, что все файлы закоммичены в git
3. Убедитесь, что `package.json` находится в `frontend/`

### Ошибка: "Build failed"

**Проблема:** Ошибки компиляции TypeScript или Next.js

**Решение:**
1. Проверьте логи сборки на Vercel
2. Убедитесь, что все зависимости установлены
3. Проверьте `next.config.js` на ошибки

### Ошибка: "Environment variables not found"

**Проблема:** Переменные окружения не установлены

**Решение:**
1. Убедитесь, что все переменные добавлены в Vercel
2. Переменные должны начинаться с `NEXT_PUBLIC_` для клиентской части
3. После добавления переменных передеплойте проект

### Предупреждения о deprecated пакетах

**Это нормально!** Предупреждения о `rimraf`, `inflight`, `glob` и других пакетах не критичны. Они не блокируют деплой.

## ✅ Чек-лист перед деплоем

- [ ] Код запушен на GitHub
- [ ] Root Directory установлен в `frontend`
- [ ] Все Environment Variables добавлены
- [ ] `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` установлен
- [ ] `NEXT_PUBLIC_API_URL` указывает на правильный backend (или localhost для теста)
- [ ] `NEXT_PUBLIC_CHAIN_ID` правильный (84532 для Sepolia, 8453 для Mainnet)

## 🔄 После деплоя

### Обновите README

Добавьте ссылку на задеплоенный фронтенд:

```markdown
## 🌐 Live Demo

- **Frontend:** https://basechainverify.vercel.app
```

### Проверьте работу

1. Откройте задеплоенный URL
2. Подключите кошелек
3. Протестируйте функциональность

## 📝 Настройка для Mainnet

Когда будете деплоить на mainnet:

1. Обновите Environment Variables в Vercel:
   - `NEXT_PUBLIC_CHAIN_ID=8453`
   - Обновите адреса контрактов на mainnet адреса
2. Передеплойте проект

---

**Удачи с деплоем! 🚀**


