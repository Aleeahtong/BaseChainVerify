# 🔧 Troubleshooting Vercel Deployment

Решение проблем при деплое на Vercel.

## ⚠️ Предупреждения (не критично)

Предупреждения о deprecated пакетах (`rimraf`, `inflight`, `glob` и т.д.) **не блокируют деплой**. Это нормально и можно игнорировать.

## ❌ Типичные ошибки и решения

### 1. Ошибка: "Cannot find module" или "Module not found"

**Причина:** Vercel не может найти файлы проекта

**Решение:**
1. Убедитесь, что **Root Directory** установлен в `frontend`
2. В настройках проекта Vercel:
   - Framework: Next.js
   - Root Directory: `frontend` ⚠️
   - Build Command: `npm run build`
   - Output Directory: `.next`

### 2. Ошибка: "Build failed" или "TypeScript errors"

**Причина:** Ошибки компиляции

**Решение:**
1. Проверьте логи сборки полностью (прокрутите вниз)
2. Убедитесь, что локально сборка проходит:
   ```bash
   cd frontend
   npm run build
   ```
3. Если локально работает, проверьте Environment Variables в Vercel

### 3. Ошибка: "NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID is required"

**Причина:** Не установлен WalletConnect Project ID

**Решение:**
1. В Vercel → Settings → Environment Variables
2. Добавьте: `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=ваш_id`
3. Передеплойте

### 4. Ошибка: "Cannot read properties of undefined"

**Причина:** Отсутствуют Environment Variables

**Решение:**
Добавьте все переменные из `frontend/.env.local` в Vercel:
- `NEXT_PUBLIC_CHAIN_ID`
- `NEXT_PUBLIC_VERIFY_CORE_ADDRESS`
- `NEXT_PUBLIC_ISSUER_REGISTRY_ADDRESS`
- `NEXT_PUBLIC_CERTIFICATE_NFT_ADDRESS`
- `NEXT_PUBLIC_API_URL`
- `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID`

### 5. Ошибка: "Webpack compilation failed"

**Причина:** Проблемы с webpack конфигурацией

**Решение:**
1. Проверьте `next.config.js` на синтаксические ошибки
2. Убедитесь, что `webpack` импортирован правильно
3. Попробуйте временно упростить `next.config.js`:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  webpack: (config) => {
    config.resolve.fallback = {
      fs: false,
      net: false,
      tls: false,
    };
    return config;
  },
}

module.exports = nextConfig
```

## 📋 Чек-лист для успешного деплоя

### Перед деплоем:

- [ ] Код запушен на GitHub
- [ ] Локальная сборка проходит: `cd frontend && npm run build`
- [ ] Все файлы закоммичены
- [ ] `package.json` находится в `frontend/`

### Настройки Vercel:

- [ ] Root Directory: `frontend`
- [ ] Framework: Next.js
- [ ] Все Environment Variables добавлены
- [ ] `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` установлен

### После деплоя:

- [ ] Проверьте логи сборки (если ошибка)
- [ ] Откройте задеплоенный URL
- [ ] Проверьте консоль браузера на ошибки

## 🔍 Как посмотреть полные логи ошибки

1. На Vercel откройте Deployment
2. Нажмите на failed deployment
3. Прокрутите вниз до секции "Build Logs"
4. Найдите строку с "Error:" или "Failed:"
5. Скопируйте полную ошибку

## 💡 Быстрое решение

Если ничего не помогает:

1. Упростите `next.config.js` (уберите IgnorePlugin временно)
2. Убедитесь, что Root Directory = `frontend`
3. Добавьте все Environment Variables
4. Передеплойте

---

**Если проблема не решается, пришлите полные логи ошибки из Vercel!**


